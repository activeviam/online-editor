import { {{toTitleCase grammarRoot}}Context } from "./{{grammarName}}Parser";

import getLexer from "./get{{toTitleCase grammarName}}Lexer";
import getParser from "./get{{toTitleCase grammarName}}Parser";

export default function getParseTree(code: string): {{toTitleCase grammarRoot}}Context {
    const lexer = getLexer(code);
    const parser = getParser(lexer);

    return parser.{{grammarRoot}}();
}
